# Homework 4 for our WebDev course at UT.ee

## Dependencies

- Node
- Postgres (directly or trough Docker using the `docker-compose.yaml`)

## Frontend

`npm i` install dependencies
`npm run serve` run vue dev server

## Backend

`npm i` install dependencies
`npm run serve` run nodemon
